import { MenuContainer } from "./Menu.styles";

const Menu = () => {
  return (
    <MenuContainer>
      <nav>
        <a>Projects</a>
        <a>About</a>
        <a>Contact</a>
      </nav>
    </MenuContainer>
  );
};

export default Menu;
